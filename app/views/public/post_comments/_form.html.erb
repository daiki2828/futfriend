<%= form_with model: [post, post_comment],  local: false do |f| %>
  <%= f.text_area :comment, rows: '3', placeholder: "コメントをここに", class: "form-control" %>
  <%= f.submit "送信", class: "btn mt-2 btn-primary", style: "float: right; border-radius: 20px;" %>
<% end %>